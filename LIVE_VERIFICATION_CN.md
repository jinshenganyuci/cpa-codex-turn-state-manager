# 本地测试记录 · 2026-09-18

## 结论

初版可运行，未获得真实的 292 字符状态。全部请求指定 `gpt-6-astra`，思考强度 `medium`。不把长度等同于推理质量、服务档位或密码学有效性。

包含排查阶段和最终复测，总共 **17 次真实请求，17 次完整返回**：

| 出口 | 次数 | 实际观察 |
|---|---:|---|
| 用户提供的第一个 SOCKS5 代理 | 12 | 332 × 4、356 × 6、WebSocket 未采集到状态 × 2 |
| 用户提供的第二个 SOCKS5 代理 | 5 | 356 × 4、WebSocket 未采集到状态 × 1 |

其中正式双代理采样为 10 次：每个代理分别测试 HTTP SSE、HTTP 非流式、WebSocket、两次后台探测。另有初始排查 3 次、流式记录排查 2 次和最终版本复测 2 次。

**真实 292 档案数：0。** 当前策略只接纳 10 个密文块，332/356 没有提升成可用状态。WebSocket 的 0 只代表插件没有采集到状态，不能证明服务端没有生成状态。没有对真实账号执行缓存重放效果测试，因为尚无符合本次目标的有效缓存。

补充：凭据的 `id_token` 与 `access_token` 中账号方案字段均为 `team`。Tonkic 参考插件的默认策略把 Team 设为 12 个密文块，通常编码为 332 字符；292 不是所有账号通用的正常标准。该基线是参考插件的经验策略，不是 OpenAI 官方规范。本次仍严格按用户指定的 292 目标接纳和保存。

## 请求与代理证据

- CPA 镜像：`jinshenganyuci/cli-proxy-api:codex-identity-v7.3.6.1`。
- 镜像 ID：`sha256:dc0b44d8dd601d76ec103a9fdf18d4ab5ecd4c29ca2a203df9b8e488245b1f5d`。
- 镜像内 CPA：`v7.3.6-codex-identity.1`，commit `7750a812a8df323e62b05586b3376a670a63ccc2`。
- 测试容器使用 `--network none`，只存在 `lo`。凭据副本使用容器回环 SOCKS 地址，Unix socket 中继只连接当前选定的用户代理。SOCKS 认证仍发送给真实代理；没有直连回退。
- 第一代理未获得目标后，切换第二个获授权代理。最后两次流式/非流式复测使用第一个代理。
- 客户端检查真实 `response.completed` 和 `status: completed`；探测代码也要求完整成功结束。模型/强度是请求条件，不声称由状态长度独立证明。
- 修复前曾漏记 SSE 结束事件；最终版本两次复测的宿主完成事件与插件终止事件均为成功。正式结果区分上游请求成功和候选状态通过接纳策略。
- 测试使用独立配置、凭据副本与容器，没有修改正常服务或 CPA 源码。

最终插件二进制 SHA-256：

```text
711d12739a6ccd0aa7110e5bce2731166da38b2ede7bffb72a64305a2e0abf53
```

## 自动测试与模拟上游

- `go test -race ./...`：通过。
- `go vet ./...`：通过。
- CPA 源码 `go build -o /tmp/cpa-manager-test-host ./cmd/server`：通过。
- 真实 CPA 进程 + 本地模拟上游：HTTP/SSE 采集、原头保留、312→292 的上游实收值、非流式响应、WebSocket 元数据、管理鉴权和静态页面均通过。
- 分别使用工作区 CPA 构建和镜像中提取的 CPA 二进制验证集成。
- 覆盖凭据/模型隔离、凭据替换、重试候选清除、过期拒绝、失败/不完整响应拒绝、私密存档权限、代理池显式授权、失败切换、无代理拒绝、429 退避、每小时预算、后台取消、实时钟调度和 SSE 分片。
- 292 的结构、替换、存档和读取测试使用模拟 Fernet 形状值。这些值不具备真实服务端签名，不能作为真实采样成果。
- 桌面 1440px、手机 390px 页面检查通过，页面没有横向溢出；表格在自己的容器内横向滚动。`desktop-demo.jpg` 是模拟数据预览。

## 已知限制

HTTP 请求头转发依赖宿主的动态映射，安装说明给出了配置。WebSocket 已建立连接不能中途替换握手头。当前版本按凭据/账号/模型隔离，未实现可靠的逻辑回合冻结、自动学习基线或首请求等待。跨回合重放为实验功能，默认关闭。

状态最长一小时是插件本地策略。若以后得到合格的 292，插件会将原值存入私密目录并保留档案；过期档案不自动重新使用。凭据、代理密码和完整状态值均不包含在源代码、安装包或公开测试报告中。
