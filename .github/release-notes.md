新增可配置的多代理并行探测，默认同一凭据最多 3 路并发。

- 优先使用上一批未用过的可用代理，其余公平随机分配。ABCD 中本轮 ABC，下一轮必带 D，再补两条。
- UI「获取设置」可设 1–128 路，设置持久保存；同一凭据的不同模型共享上限。
- 任一路取得合格 292 / 332 后取消其余未完成探测，已完成结果仍优先 292。
- 正常业务请求继续使用凭据自己的代理；无缓存默认放行。原有时间设置、勾选和缓存保留。
- 本地构建，仅 Linux amd64 安装包和 SHA256 校验文件。
