新增可持久保存的获取时间设置，默认到期前 30 分钟准备候选值、每 7 秒重试。

- UI 可自定义提前 1–59 分钟获取、1–3600 秒重试，支持保存、重新载入和恢复默认。
- 保存立即生效并重新计算预计刷新，重启和升级后保留；已保存 UI 设置优先于旧 YAML。
- 调度器按重试截止时间唤醒，7 秒不会因 5 秒检查周期变成 10 秒。网页数据仍每 5 秒刷新。
- 不清空当前/备用值，不改变已勾选范围、代理路由、292 优先或无缓存放行规则。
- 本地构建，仅发布 Linux amd64 安装包和校验文件，不运行 GitHub Actions。
