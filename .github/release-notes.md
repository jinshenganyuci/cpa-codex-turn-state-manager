最近请求的「请求出口」改为上下两行：代理名称在上，出口 IP 在下。

- IP 通过该次探测的同一条 TLS 连接查询；查询不允许重新拨号或跳转，不携带 OAuth、不调用模型。
- 每次获取仍独立连接，保留轮换代理行为。无法确认或旧记录没有 IP 时显示「IP 未获取」。
- 保存每条请求的出口 IP 和代理名称，重启后保留；15 秒获取间隔和业务凭据代理规则不变。
- 本地构建，仅发布 Linux amd64 安装包和校验文件，不运行 GitHub Actions。
