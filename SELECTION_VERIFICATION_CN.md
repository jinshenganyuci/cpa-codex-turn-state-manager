# 0.1.3 模型与凭据勾选验证

## 行为

界面提供“参与获取的模型”和“参与获取的凭据”两组复选框。两者均勾选的组合才会自动获取、接受手动探测、采集成功值和使用缓存。Astra 与 Sol 的状态仍按凭据、账号身份和模型分别隔离。

缺少合格值时，以配置的 5 秒最短间隔持续尝试；一个后台工作线程顺序处理到期任务，实际开始时间还受正在进行的请求与退避影响。得到完整成功的 292/332 后保存完整值，暂停获取，接近 55 分钟时刷新。取消勾选会取消该组合的探测，丢弃迟到结果并停止替换，保留既有缓存和档案。

选择以独立 `selection_file` 原子保存，权限 `0600`。重载或重启可恢复；新安装默认空选择，新凭据不会自动加入。凭据选择绑定账号身份；同名文件换成另一个账号后，原勾选不能继续授权该账号。

## 验证

- `go test -race ./...`、`go vet ./...`、原生共享库构建：通过。
- 未勾选不探测、不缓存、不替换；仅勾选凭据或仅勾选模型不生效：通过。
- Astra / Sol 独立获取并缓存，未勾选凭据不发送请求：通过。
- 取消探测后迟到值不提升、不归档；进行中的业务响应在取消选择后不提升：通过。
- 重载恢复、全部取消后仍保持关闭、账号替换隔离、非法模型和凭据拒绝、写盘失败不改变生效状态、版本冲突返回 409：通过。
- 浏览器勾选、保存、刷新恢复、取消模型、取消凭据、草稿不被轮询覆盖、版本冲突提示、移动端不溢出：通过。
- 真实 CPA 管理接口鉴权、保存选择和配置重载、双长度头转发与归档、从 0.1.2 原生热升级至 0.1.3：通过。集成测试使用本地模拟上游，不把合成状态当作真实上游值。

## 常驻实例

容器 `cpa-turn-state-daemon` 已热加载 **0.1.3**；CPA PID 保持 **668270**，启动时间保持原值，没有重启或关闭 CPA。当前 5 秒间隔、原凭据代理及原缓存保留。

升级只保留之前已经启用的 `gpt-6-astra` 和原凭据。`gpt-5.6-sol` 显示为未勾选，用户可自行选择。实际浏览器验证时只临时修改了未保存的 Sol 勾选，最终保存的仍是 Astra；没有发送未勾选 Sol 的探测请求。

实际页面已完成登录、保存原选择、重新载入、桌面及手机布局验证。管理密钥输入框在截图前清空，截图不包含凭据密钥或原始状态。

界面：http://127.0.0.1:18317/v0/resource/plugins/codex-turn-state-manager/status

截图：`dist/selection-ui-0.1.3.png`、`dist/selection-ui-0.1.3-mobile.png`。

插件 SHA-256：`f0f045c798b105a98d24cf6094928fb15bc60e3b9cb07568ee9e924911c831e6`。

CPA 主仓库和其他运行服务未修改。GitHub 发布继续暂停。
