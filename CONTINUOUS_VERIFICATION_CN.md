# 0.1.2 持续获取与常驻运行验证

## 行为

启用 `probe.continuous: true` 后，后台线程自动发现已加载或稍后加载的 Codex OAuth 凭据和明确配置的模型，不需要先发送业务请求。

没有合格值时持续尝试：网络错误、缺少状态或返回 356 等不接受的长度，不会使待获取任务丢失。持续模式不受每小时或累计次数上限限制；普通尝试最少间隔 10 秒，调度器每 5 秒检查一次。每次只发一个后台探测，使用 `gpt-6-astra / medium` 和最短提示。

获取到完整成功的 292 或 332 后保存完整值，使用新缓存，停止持续探测。签发约 55 分钟时再次刷新；满 60 分钟不再使用旧值。刷新失败时，未过期旧值继续可用。HTTP 401/403、429 与已识别的配额错误使用 15 分钟退避，退避后恢复尝试；凭据刷新仍由 CPA 负责。

`max_per_hour` 仅在关闭 continuous 时生效；持续模式状态 API 中 `max_per_hour: 0` 表示无小时次数上限。页面取消只中断当前探测，已启用的自动获取随后继续。

## 本地验证

- `go test -race ./...`、`go vet ./...`、前端语法检查、原生共享库构建：通过。
- 可控时钟验证：冷启动无业务请求获取、失败及 356 后重试、超过旧小时上限仍继续、两种目标均可终止获取、55 分钟再次刷新、限流退避后恢复：通过。
- 独立真实 CPA 加本地模拟上游的 HTTP/SSE/WebSocket 生命周期、完整 292/332 请求头转发及私密档案：通过。
- 常驻真实凭据测试：实例启动后没有调用业务接口或手动探测接口，自动产生了 **17 次**探测记录，已超过旧的每小时 6 次限制。检查时间：`2026-09-19T05:05:03.775099+00:00`。
- 检查时状态长度分布：`{0: 1, 356: 16}`；0 是没有获得状态的内部标记。有效缓存数：`0`。结果记录随后台运行继续变化。
- 管理页面 HTTP 200；未认证的管理数据访问为 HTTP 401。

## 保持运行的实例

容器名：`cpa-turn-state-daemon`。镜像：`jinshenganyuci/cli-proxy-api:codex-identity-v7.3.6.1`。插件：`0.1.2`。重启策略：`unless-stopped`；Docker 系统服务已启用开机启动。本次结束后保留运行。

本地 API：`http://127.0.0.1:18317/v1`。

插件界面：`http://127.0.0.1:18317/v0/resource/plugins/codex-turn-state-manager/status`。

运行目录：`/root/codex-private/cpa-turn-state-daemon`。管理密钥在 `management-key.txt`，业务密钥在 `client-key.txt`，均为私密文件。原始凭据、代理口令和状态不进入安装包或报告。凭据和全局代理均指向用户提供的第一个 SOCKS5；探测使用 credential 模式，失败不直连。临时生成的实际 YAML 在容器 tmpfs 中，长期模板不含管理密钥、业务密钥或代理口令。

已启动的旧 CPA、其他服务及 CPA 主仓库保持原状。GitHub 发布仍暂停。

插件 SHA-256：`a935d708c6c2588adbccab14d6c39eb79a0edb85053c9fba4b6661301795a56c`。
