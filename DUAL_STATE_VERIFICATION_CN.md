# 0.1.1 双长度采集与刷新验证

本次仅修改本地插件、测试驱动和安装配置。CPA 主仓库保持干净，运行中的服务未改动，未发布 GitHub。本次没有向真实 OpenAI 发出额外请求。

## 新行为

- 未显式设置长度策略时接受 292 和 332；配套配置显式设置 `accepted_blocks: [10, 12]`。旧配置中显式的 `[10]` 保持原意，升级需要合并新版配置。
- 两种长度均保存完整原始值，当前缓存支持重启恢复，档案权限为 `0600`。默认策略不采用 312 / 356。
- 配套配置使用 `force`，获得哪一种合格值就使用哪一种。签发时间更新的成功值会接替旧值，同一签发秒内的新值也能接替；旧签发时间、失败或未完整结束的结果不能覆盖当前缓存。
- 从值的签发时间计算，约 55 分钟开始后台刷新，60 分钟停止复用；后台每 5 秒检查。不是从每次使用时重新计算一小时。
- 刷新失败保留尚未过期的旧值，至少 60 秒间隔重试，每凭据每小时最多 6 次；限流有退避。旧档案过期后仍保留。
- 界面分别展示 292 / 332 的近期成功次数、当前缓存长度和预计刷新时间。
- 更新了本地采样工具，后续合格 332 不再只记录长度。

## 验证结果

| 验证 | 结果 |
|---|---|
| `go test -race ./...` | 通过 |
| `go vet ./...` | 通过 |
| `node --check web/app.js` | 通过 |
| 原生共享库构建 | 通过 |
| 292 / 332 完整值保存、私密档案、重启恢复 | 通过 |
| 332 → 292 → 332 接替，同秒值接替，旧值和失败响应不覆盖 | 通过 |
| 54 分 59 秒不刷新、55 分钟刷新，任一目标值即停止探测 | 可控时钟测试通过 |
| 刷新失败保留有效旧值、60 秒重试、满一小时停止复用 | 可控时钟测试通过 |
| 真实 CPA HTTP/SSE 与 WebSocket 生命周期、管理鉴权 | 通过 |
| 真实 CPA 上游接收完整 332 和 292，以及完整档案落盘 | 通过 |

真实 CPA 测试在 `--network none` 容器中运行，使用本地模拟上游和无有效签名的合成状态。测试验证插件行为及真实宿主转发，不代表这些合成值可用于真实上游，也不是实际等待一小时的结果。

集成宿主：`v7.3.6-codex-identity.1`，提交 `7750a812a8df323e62b05586b3376a670a63ccc2`。首次检查出现初始化期间的缓存等待超时；测试增加模型目录及 watcher 就绪条件后，两项集成测试连续三轮全部通过，共 6 次。

插件 SHA-256：`7c76b2f3885e9d04ff675791d51fa641307b248635befc19cf811026913428ed`。

## 安装注意

用新版共享库替换旧插件，并合并新版 `config.example.yaml`。仅替换二进制不会覆盖旧 YAML 中显式的只观察、`[10]` 或关闭探测配置。HTTP 宿主仍需要文档中的 `X-Codex-Turn-State` 凭据头映射，参见 `README_CN.md`。

缺失值由下一次符合条件的业务请求触发异步获取，不阻塞该请求。业务请求继续绑定原凭据代理；探测默认也继承该代理，失败不直连。采集长度不能证明模型质量，一小时是本地使用策略。
