# 0.2.0：292 优先、业务回收与备用切换验证

验证日期：2026-09-19。本地插件 0.2.0，未发布 GitHub。具体时间和摘要见 `priority-verification.json`。

## 实现结果

1. 模型一致才入库：完整响应成功、状态结构与签发时间合格、终态报告模型和实际执行模型一致。缺少证据或模型不一致时拒绝缓存；别名按执行模型比较。
2. 292 优先：332 可先保底使用，但继续寻找 292；拿到 292 立即升级。后续 332 不覆盖有效 292，备用槽同样优先 292。
3. 当前与备用分开保存：到期前准备下一份值，到期时先切换有效备用，再判断是否需要拦截。较旧响应不覆盖同类更新值。
4. 业务观测回收：业务响应返回不同的新 292，经过相同验收后可进入备用槽，来源显示“业务观测”。当前只有 332 时直接升级。重复返回同一个值不更新签发时间、捕获时间或原来源。
5. 凭据专属探测代理池：只使用显式配置的授权出口，固定出口失败冷却、成功短暂轮休，支持启用、停用和解除冷却；配额和认证问题独立退避。代理设置持久保存成功后才生效，失败不改变运行设置。
6. 持久记录：当前/备用值、最近 200 条请求、代理健康、探测预算及退避使用私密文件保存，UI 显示验收结果与回收决定，不返回完整状态和凭据密钥。

## 自动验证

- `go test -race ./...`、`go vet ./...`、原生动态库构建通过。
- 新单元场景覆盖：292 优先和到期接替、业务回收完整成功/失败/模型缺失/不一致、迟到错误不删除另一个新缓存、332 后继续获取 292、历史和备用重新加载、显式代理池轮换和冷却、保存失败原子性、旧缓存不得臆造模型证据。
- 额外使用可控时钟验证：当前 292 到 55 分钟时，已有更新的业务备用 292 能避免探测；重复回显不改变备用值的时间或来源。
- 7 项真实 CPA 集成场景通过，宿主版本 `v7.3.6-codex-identity.1`。测试容器使用 `--network none` 和本地合成上游，覆盖已有 HTTP/SSE/WebSocket 行为、双长度档案、选择保存、0.1.6 → 0.2.0 原生热升级、邮箱/模型显示、请求拦截，以及新增业务 292 优先/备用/模型门控。
- 新原生场景实际检查上游收到的完整请求头：332 → 292 升级；第二个 292 留作备用；后续 332 不覆盖；同值重复记录；模型不一致时使用备用接替；备用也被明确判定不匹配后，后续请求 503 且未到达合成上游。
- 浏览器覆盖 17 项选择/展示/代理操作检查和 15 项自动登录检查；移动端无页面横向溢出、无控制台错误。

合成状态只有 Fernet 外形，不能用于真实 OpenAI 请求。这些测试证明插件行为，不证明真实上游会签发 292，也不验证上游状态签名或模型能力。

## 本地常驻实例

- 已热加载 0.2.0，CPA 容器 PID 与启动时间保持原值；没有重启 CPA，也没有修改 CPA 源代码。
- 凭据选择文件逐字节保持一致，仍为原凭据和用户勾选的 `gpt-6-astra`、`gpt-5.6-sol`。
- 两个已授权 SOCKS5 出口只加入该凭据的后台探测允许列表，业务凭据代理不变。默认思考强度 medium、普通最小间隔 5 秒；固定出口 3 次普通失败后冷却 60 秒，成功轮休 5 秒。
- 旧实例捕获到的 198 条历史迁入新日志；后续按最近 200 条保留。旧状态没有保存响应模型验收证据，不直接作为新严格模式的有效缓存；原始档案与升级前快照保留在私密目录。
- 截至本次记录，两条出口各完成 12 次探测，尚无新验收通过的 292 / 332。观察到 356 与 Astra 请求报告 Luna，均未进入有效缓存。
- 对运行中的 CPA 发起一次 Astra / medium 短请求，得到插件的 `valid_turn_state_required` 503；没有合格缓存时拦截生效。后台保留运行，冷却结束会继续获取。
- 局域网 `192.168.7.249:18317` 浏览器实测正常，自动恢复登录、两模型勾选和两代理行均正确。截图未加入安装包，以免包含邮箱与代理地址。

模型一致验收依赖上游报告字段。业务流式响应到终态才确认模型，无法撤回之前已经发出的内容；发现不一致后只处理本次使用的缓存和后续请求。292 / 332 长度本身不是模型能力证明。
