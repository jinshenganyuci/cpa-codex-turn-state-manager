# 0.1.4 无有效状态拦截验证

日期：2026-09-19。仅本地部署，未发布 GitHub。

- `go test -race ./...`、`go vet ./...`、原生动态库构建通过。
- 单元验证：缺失、恰好一小时过期、超前时间、312、356、非法结构、身份不符、身份查询失败、凭据替换和不匹配策略均拦截。未勾选范围与观察/模拟模式保持原行为；新获取 332 后自动放行，过期再次拦截。292 与 332 在刷新窗口均继续可用，探测仍使用凭据代理。
- 使用当前 CPA 镜像在 `--network none` 的独立容器运行原生集成：生命周期、双长度捕获/存档/转发、勾选持久化、0.1.3 到 0.1.4 热更新和拦截恢复均通过。
- 模拟上游捕获证明：Responses 和 Chat Completions 的普通与 SSE 请求返回 503，WebSocket 连接关闭；被拦截请求到达上游的数量为 0。捕获有效 332 后，请求恢复，实际发出的请求头与缓存逐字一致。
- 当前 CPA 对已升级 WebSocket 的终止直接关闭连接，不返回 JSON 错误帧；插件记录已验证为 `blocked_before_upstream`。无需修改 CPA。
- 真实运行实例一次 `gpt-6-astra / medium` API 验证：有效缓存 0，HTTP 503，`valid_turn_state_required`，`Retry-After: 5`，耗时 0.0032 秒，插件记录“未发送上游”。本次拦截没有执行上游推理。
- 真实浏览器登录与模拟浏览器测试通过：显示拦截状态和拦截历史，勾选、保存、刷新、冲突处理与移动端布局正常。
- 容器 `cpa-turn-state-daemon` 保持 PID 668270，启动时间未变。代理策略与原勾选文件保留，持续获取开启，重试间隔仍为 5 秒，配额等退避规则保留。

该长度检查不验证 HMAC，也不能证明模型能力。模拟状态只存在于隔离测试环境，从未写入真实凭据或缓存。宿主关闭/卸载/熔断插件时，该插件无法拦截业务流量。原始状态与凭据不包含在本验证文件或安装包内。
