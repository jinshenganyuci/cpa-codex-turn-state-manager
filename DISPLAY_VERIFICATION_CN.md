# 0.1.6 凭据与响应模型显示验证

日期：2026-09-19。仅本地部署，未发布 GitHub。

- 凭据勾选、缓存列表、最近请求显示邮箱和套餐。识别 Pro、Plus、Free、Team、Business 等；套餐来自凭据记录和身份令牌声明，不通过文件名猜测，不代表实时订阅核验。仅输出展示字段，不输出令牌或原始声明。
- 请求模型保留客户端别名，执行模型仍用于隔离缓存；响应模型读取 JSON 字段，保存来源。原始 WebSocket 事件优先。HTTP Responses 不使用可能由 CPA 补入的初始模型；转换后的 Chat Completions 流块无法确认时显示“未提供”。
- 单元测试覆盖模型不一致、缺失字段、生成文字不被当作模型证据、跨分块终态、重试清除旧模型、凭据显示快照、探测未拿到状态值仍记录模型，以及多种套餐和损坏 JWT。
- `go test -race ./...`、`go vet ./...`、原生动态库构建通过。
- 当前 CPA 镜像在 `--network none` 独立容器运行 6 项原生集成测试全部通过：生命周期、双长度捕获/回放、勾选持久化、原生热升级、凭据与模型展示、请求拦截。模拟上游报告 `gpt-5.6-luna`，普通与流式 Responses 及 WebSocket 均正确记录请求模型和响应模型，未使用请求值覆盖响应值。
- 浏览器验证邮箱、Plus 套餐、模型不一致突出显示、新表头、移动端布局、勾选保存、登录恢复均通过；自动登录回归 15 项通过。
- 实际运行的一条 `gpt-6-astra / medium` 流式短请求返回 HTTP 200，耗时 3.207 秒，回答 OK。插件记录请求 `gpt-6-astra`、响应 `gpt-6-astra`，与最终响应字段完全一致；凭据显示邮箱与 Team 套餐。完成记录由宿主异步回调写入，检查时等待回调，无重复发送推理请求。
- 已验证公网资源加载最新版本，管理 API 未授权仍返回 401。常驻 CPA PID 668270 未变，勾选范围未变，自动登录、凭据代理、持续获取、5 秒重试和缺少有效缓存拦截保持生效。
- 热更新通过显式写入原有默认 `auto_update: true` 触发，有效行为未改变。旧版本动态库保留以便回滚。

响应模型是返回字段的报告，不能证明底层权重或模型能力。旧请求没有采集过的模型信息不能回溯猜测。实际邮箱只在受管理鉴权保护的状态接口与本地私有验证记录中出现，不放入发布包的验证报告。
