# 0.1.5 自动登录验证

日期：2026-09-19；本地热更新，未发布 GitHub。

- 登录浏览器测试 15 项通过：首次登录、错误密钥不保存、服务端验证成功后保存并隐藏输入栏、刷新、新标签页、浏览器数据恢复、网络失败保留登录、401 清除失效登录、继承 CPA 混淆存储、CPA iframe 免二次登录、主面板退出后的会话清理、跨来源配置不使用、损坏数据回退、明文旧格式兼容、更换密钥。
- 原有勾选范围、保存/取消、冲突处理、移动端布局、拦截状态与历史浏览器测试通过。
- `go test -race ./...`、`go vet ./...`、原生动态库构建通过。
- 当前 CPA 镜像在隔离网络容器中验证 0.1.4 到 0.1.5 原生热升级与 HTTP/SSE/WebSocket 请求拦截回归通过。
- 实际浏览器通过 CPA 主面板登录并勾选记住密码，进入插件、刷新主页面、单独打开插件，均自动连接并隐藏输入栏，页面脚本错误 0。
- 未登录调用插件管理 API 仍返回 401；密钥不嵌入公开资源或安装包。
- 常驻 CPA PID 668270 未变，勾选文件保持不变，持续获取、凭据代理、5 秒重试和无有效缓存拦截保持生效。
- 空配置 PATCH 在当前 CPA 不触发动态库替换，因此显式保存原有默认 `max_state_bytes: 4096` 后完成热升级；该项有效行为未变。

新的浏览器首次连接仍需要一次登录；CPA 未保存密码时无法从其持久存储恢复密钥。浏览器保存采用可逆混淆，不能视作密钥加密。来自 CPA 的登录不复制到插件存储，单独登录只在验证成功后保存。
