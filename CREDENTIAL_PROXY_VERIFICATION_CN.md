# 0.2.1：凭据代理与并发获取验证

验证日期：2026-09-20（Asia/Shanghai）。本地版本，未发布 GitHub。

## 最终行为

- 插件后台获取只读取对应 CPA 凭据中的 `proxy_url`，每次获取重新读取。凭据代理缺失、无效或失败时，不切换独立代理或直连。
- 删除独立代理池界面、操作接口与运行状态；旧的代理池、第一跳、覆盖和代理健康配置仅兼容解析，随后丢弃，不再决定路由。旧代理冷却和停用记录不限制获取。
- 不同凭据、不同模型独立并行获取，同一“凭据 + 模型”组合最多运行一个任务。手动获取立即启动并返回 `202 / started`，重复提交立即返回 409，重试或退避期立即返回 429；手动请求不入队。
- 正常 API 请求不等待后台获取：有合格缓存就转发，无合格缓存则沿用 503 拦截。自动触发仅更新刷新需求，不保留等待中的业务请求。
- 取消某个组合只取消对应获取，其他组合继续；整体取消、重新加载和停止插件会取消并等待所有已启动任务退出，避免遗留后台工作。
- 292 优先、332 保底继续寻找 292、业务观测回收、备用接替、模型一致验收、历史保存、账号/模型勾选与自动登录保留。
- 普通重试间隔仍是 5 秒；认证、配额和上游限流退避保留。并行获取不保证上游没有自身的等待或处理延迟。

## 自动验证

`go test -race ./...`、`go vet ./...`、原生动态库构建通过。

新回归覆盖：旧池和第一跳无法覆盖凭据代理、修改凭据后读取新代理、连续普通失败不触发已删除的池冷却、缺失/无效代理失败即停、旧池日志不恢复停用、认证/配额退避仍保留、已移除的代理 API 返回 404。

并发测试使用通道阻塞模拟请求：两个凭据 × 两个模型共四个任务均在任一任务完成前启动；重复触发不生成额外任务；单模型取消不取消另一个模型；后台尚未返回时，业务请求已获得明确 503，未等待探测。测试使用同步屏障与可控时钟，不依赖 TTL 睡眠。

真实 CPA 宿主配本地模拟上游的 **9 项集成测试全部通过**：原有生命周期、双长度档案、勾选、0.2.0 → 0.2.1 热升级、显示、拦截及 292 优先/业务备用回归，以及新增旧池兼容与业务请求并行测试。并行业务测试实际看到两个携带缓存的请求均到达上游，随后才释放第一个响应；并未仅凭两个客户端同时启动就判断不排队。

浏览器通过 **19 项选择/展示检查**与 **15 项登录检查**，覆盖移除池面板、凭据代理说明、并行提示、手动启动、保留选择、自动登录和移动端布局，无控制台错误。

## 常驻实例验证

- CPA 容器 PID 与启动时间保持原值，没有重启 CPA。运行中的插件版本确认为 0.2.1，`probe_parallel: true`、`proxy_mode: credential`，不再返回 `proxies` 管理数据。
- 两模型和原凭据的选择文件逐字节保持一致；凭据本身的代理设置保持不变。运行配置及启动模板均已移除独立代理字段，原池文件移入私密升级备份。
- 实际观察到 **2 个获取同时进行**，进程同时有 **2 个连接指向凭据代理**，没有连接已移除的备用代理。此证据验证并行派发及出口选择，不证明取得合格 state。
- 无合格缓存时，同时调用 Astra / medium 与 Sol / medium，两请求分别约 **62 毫秒、86 毫秒**返回 `503 / valid_turn_state_required`，后台保持获取。
- 局域网浏览器实测：代理池面板消失，登录自动恢复，凭据/模型勾选保留。当前仍未采到新的合格 292 / 332，没有把 356 或模型不匹配当作成功缓存。
- 热更新时发现仅删除嵌套配置字段未触发宿主加载新库。通过临时改变再恢复原状态长度上限，触发实际加载；最终长度上限恢复原值。以运行版本、API、浏览器与进程连接确认生效。

详细机器可读结果见 `credential-proxy-verification.json`。截图包含邮箱，未放入安装包。凭据、代理密码、完整 state 与管理密钥均不进入安装包。
